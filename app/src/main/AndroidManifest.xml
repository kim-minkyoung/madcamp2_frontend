<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:launchMode="singleTop"
        android:theme="@style/Theme.Madcamp2_frontend"
        tools:targetApi="31">
        <activity
            android:name=".view.activity.RankingActivity"
            android:exported="false"
            android:label="@string/title_activity_ranking"
            android:theme="@style/Theme.Madcamp2_frontend" />
        <activity
            android:name=".view.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.BeforeStartActivity" />
        <activity android:name=".view.activity.DrawingActivity" />
        <activity android:name=".view.activity.SignInActivity" />

        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.CREDENTIAL_PICKER"
            android:value="true" />
        <meta-data
            android:name="com.google.android.gms.auth.api.signin"
            android:resource="@xml/google_signin" />

    </application>

</manifest>